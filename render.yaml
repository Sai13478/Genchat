services:
  - type: web
    name: Genchat
    env: node
    buildCommand: |
      cd frontend && npm install && npm run build
      cd ../backend && npm install
    startCommand: node src/app.js
    rootDir: backend
    envVars:
      - key: NODE_ENV
        value: production
