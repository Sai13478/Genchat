{
    "name": "genchat-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build:frontend": "cd frontend && npm install && npm run build",
        "install:backend": "cd backend && npm install",
        "install:all": "npm install && npm run build:frontend && npm run install:backend",
        "start": "cd backend && node src/index.js"
    }
}